# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.production and fill in your values
# NEVER commit .env.production to git!
# =============================================================================

# =============================================================================
# DATABASE CONNECTIONS (Use managed services in production)
# =============================================================================
# PostgreSQL (Neon, Supabase, AWS RDS, etc.)
DATABASE_URL=postgresql://user:password@host:5432/auth_db?sslmode=require

# MongoDB (MongoDB Atlas, etc.)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/club_db?retryWrites=true&w=majority

# RabbitMQ (CloudAMQP, etc.)
RABBITMQ_URL=amqps://user:password@host/vhost

# =============================================================================
# JWT KEYS (Generate unique keys for production!)
# =============================================================================
# Run: .\scripts\generate-jwt-keys.ps1
# The keys are stored in secrets/production/jwt_private.pem and jwt_public.pem
# For Kong, paste the public key here (with \n for newlines):
JWT_RSA_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\nMIIBIjAN...your key here...\n-----END PUBLIC KEY-----"

# =============================================================================
# SECRETS (Generate unique values!)
# =============================================================================
# API Gateway Secret (min 16 chars) - Run: openssl rand -base64 32
API_GATEWAY_SECRET=your-api-gateway-secret-here

# Refresh Token Secret (min 32 chars) - Run: openssl rand -base64 48
REFRESH_TOKEN_SECRET=your-refresh-token-secret-here

# =============================================================================
# FRONTEND
# =============================================================================
FRONTEND_URL=https://yourdomain.com
FRONTEND_API_URL=https://api.yourdomain.com

# =============================================================================
# AWS (for S3 image storage)
# =============================================================================
AWS_REGION=ap-southeast-1
S3_BUCKET=your-bucket-name
STORAGE_PROVIDER=s3

# =============================================================================
# OPTIONAL
# =============================================================================
VERSION=latest
